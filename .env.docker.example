# ============================================================
# 🐳 ENV FILE (Ejecución con Docker Compose)
# Copia este archivo como .env.docker antes de ejecutar:
#   cp .env.docker.example .env.docker
# ============================================================

# Entorno de ejecución
APP_ENV=prod

# URL de conexión a la base de datos dentro del contenedor
# ⚠️ Nota: el host aquí es 'db' (nombre del servicio en docker-compose)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/mini_blog

# Puerto expuesto por la API
APP_PORT=8000

# Credenciales de PostgreSQL (deben coincidir con docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mini_blog

# ------------------------------------------------------------
# ✅ Recomendaciones:
# - No subas .env.docker real al repositorio (solo el .example).
# - Usa credenciales simples para desarrollo local (se destruyen al parar los contenedores).
# - En producción usa variables seguras o Docker secrets.
# ------------------------------------------------------------
