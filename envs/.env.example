# ============================================================
# 🌱 ENV FILE (Desarrollo local con Poetry + Uvicorn)
# Copia este archivo como .env antes de iniciar el entorno local:
#   cp .env.example .env
# ============================================================

# Entorno de ejecución (dev | test | prod)
APP_ENV=dev

# URL de conexión a la base de datos local (fuera de Docker)
# Reemplaza <TU_PASSWORD> por tu contraseña real de Postgres local
DATABASE_URL=postgresql+asyncpg://postgres:<TU_PASSWORD>@localhost:5432/mini_blog

# Base de datos para pruebas automatizadas (pytest)
# Reemplaza <TU_PASSWORD> por tu contraseña real de Postgres local
TEST_DATABASE_URL=postgresql+asyncpg://postgres:<TU_PASSWORD>@localhost:5432/mini_blog_test

# Puerto por defecto de la API
APP_PORT=8000

# ------------------------------------------------------------
# ⚠️ Importante:
# - No uses credenciales reales en este archivo.
# - Este archivo SÍ se versiona (es plantilla).
# - Crea tu propio .env (NO versionado) con tus credenciales reales.
# ------------------------------------------------------------
